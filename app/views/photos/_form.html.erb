<%= simple_form_for [current_user,@photo] do |f| %>

  <%= f.input :description %>
  <%= f.input :image %>
  <%= f.collection_select :album_id, Album.order(:id).where(user_id: current_user.id) , :id, :title %>
  <%= f.submit %>
<% end %>
