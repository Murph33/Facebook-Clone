var target = $("[id='<%= dom_id(@comment, :edit) %>']");


target.each(function(_,e){
  if ($(e).parents(".modal-body-text").length) {
    $(e).replaceWith("<%= j render 'comments/comment_modal', comment: @comment %>")
  } else {
    $(e).replaceWith("<%= j render partial: @comment %>")
  }
});
